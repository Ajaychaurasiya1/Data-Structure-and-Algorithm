
Node Insert(Node head,int x) {
    Node node = new Node();
    node.data = x;
    node.next = null;
    if(head == null){
        return head = node;
    }
    else{
        node.next = head;
        head = node;
        return head;
    }
    
}
